# TODO

## Bugs

- [ ] Web server fails to start when prompt files are missing — should fall back to defaults or show a clear error instead of crashing

## Features

- [ ] Prompt editing in the web UI — view and edit the LLM prompt templates without touching the filesystem
